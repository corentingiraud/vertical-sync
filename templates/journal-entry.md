# Training journal — <block / goal race>

> **Chronological** memory: dated observations, **most recent on top**.
> One file per training block, named `coach/journal/YYYY-<race>.md` (gitignored).
>
> The journal is the raw log the LLM coach reads to understand recent context
> (how training is going, what hurt, what was decided). Durable facts that
> outlive the block get distilled into `coach/athlete.md`.
>
> Copy one of the entry blocks below to the TOP of the file for each new entry.

---

## YYYY-MM-DD — <short title of what this entry captures>

<What happened / what was observed. Numbers from `vs analyze` or `vs week`,
how the body felt, what was decided and why. Be specific — this is what the
coach reads next block to remember why a choice was made.>

⚠️ <Flag anything that needs follow-up: a pain to watch, a load spike, a
decision to revisit.>

---

## YYYY-MM-DD — <previous entry>

<...>
